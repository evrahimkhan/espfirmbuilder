let csrf='',register=false;const dialog=document.querySelector('#auth');const oauthError=new URLSearchParams(location.search).get('oauth_error');if(oauthError){history.replaceState(null,'',location.pathname);dialog.showModal();queueMicrotask(()=>document.querySelector('.auth-error').textContent=oauthError);}document.querySelectorAll('[data-open-auth]').forEach(b=>b.onclick=()=>dialog.showModal());document.querySelector('.close').onclick=()=>dialog.close();dialog.addEventListener('click',event=>{const rect=dialog.getBoundingClientRect();if(event.clientX<rect.left||event.clientX>rect.right||event.clientY<rect.top||event.clientY>rect.bottom)dialog.close()});document.querySelector('.mode').onclick=()=>{register=!register;document.querySelector('.name-field').style.display=register?'block':'none';document.querySelector('.mode').innerHTML=register?'Already registered? <b>Sign in</b>':'New here? <b>Create an account</b>'};fetch('api/auth.php?action=session').then(r=>r.json()).then(d=>{csrf=d.csrf;if(d.user){document.querySelectorAll('[data-open-auth]').forEach(b=>{b.textContent='Open dashboard →';b.onclick=()=>location.href='dashboard.html'})}}).catch(()=>{});document.querySelector('#auth-form').onsubmit=async e=>{e.preventDefault();const error=document.querySelector('.auth-error');error.textContent='';const data=Object.fromEntries(new FormData(e.target));try{const r=await fetch(`api/auth.php?action=${register?'register':'login'}`,{method:'POST',headers:{'Content-Type':'application/json','X-CSRF-Token':csrf},body:JSON.stringify(data)});const out=await r.json();if(!r.ok)throw new Error(out.error);location.href='dashboard.html'}catch(ex){error.textContent=ex.message}};
